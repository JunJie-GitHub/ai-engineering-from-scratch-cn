{
  "questions": [
    {
      "stage": "pre",
      "correct": 1,
      "question": "What OS analogy does MemGPT build on?",
      "questionZh": "MemGPT 基于哪种操作系统（Operating System, OS）类比进行构建？",
      "options": [
        "Processes and threads",
        "Virtual memory: main context as RAM, external store as disk, memory tools as page in and out",
        "Network sockets",
        "File descriptors"
      ],
      "optionsZh": [
        "进程 (Processes) 与 线程 (Threads)",
        "虚拟内存 (Virtual Memory)：主上下文作为内存 (RAM)，外部存储作为磁盘 (Disk)，内存工具作为页面调入与调出 (Page In/Out)。",
        "网络套接字（Network sockets）",
        "文件描述符 (File descriptors)"
      ],
      "explanation": "MemGPT maps prompt to RAM, external store to disk, and memory tools to page-fault-style transfers.",
      "explanationZh": "MemGPT 将提示词（Prompt）映射为内存（RAM），将外部存储（External Store）映射为磁盘（Disk），并将记忆工具（Memory Tools）映射为缺页中断式传输（Page-fault-style transfers）。"
    },
    {
      "stage": "pre",
      "correct": 1,
      "question": "Why do bigger context windows not fully solve memory?",
      "questionZh": "为什么更大的上下文窗口（context window）无法完全解决记忆（memory）问题？",
      "options": [
        "Providers cap them at 4k tokens",
        "Overflow, dilution of attention, and lack of cross-session persistence still bite even with 128k windows",
        "Long context costs nothing",
        "The model refuses long input"
      ],
      "optionsZh": [
        "服务提供商将其上限设为 4k 个词元（token）。",
        "即使在使用 128k 上下文窗口 (128k Context Windows) 的情况下，上下文溢出 (Context Overflow)、注意力稀释 (Attention Dilution) 以及缺乏跨会话持久化 (Cross-session Persistence) 的问题依然会带来困扰。",
        "长上下文（Long Context）零成本",
        "模型拒绝长输入"
      ],
      "explanation": "Mem0 measured 128k-window baselines still missing facts a 4k agent with external memory catches; overflow, dilution, and persistence remain.",
      "explanationZh": "Mem0 的测试表明，128k 上下文窗口（context window）基线模型仍会遗漏事实，而配备外部记忆（external memory）的 4k 智能体（agent）却能成功捕获；上下文溢出（overflow）、信息稀释（dilution）与记忆持久性（persistence）问题依然存在。"
    },
    {
      "stage": "check",
      "correct": 3,
      "question": "Which of these is NOT one of MemGPT's canonical memory tools?",
      "questionZh": "以下哪一项不是 MemGPT 的标准记忆工具（canonical memory tools）？",
      "options": [
        "core_memory_append",
        "archival_memory_search",
        "conversation_search",
        "gradient_memory_update"
      ],
      "optionsZh": [
        "core_memory_append",
        "archival_memory_search",
        "conversation_search",
        "gradient_memory_update"
      ],
      "explanation": "There is no gradient memory tool; the surface is core/archival/conversation operations.",
      "explanationZh": "不存在梯度记忆工具 (gradient memory tool)；表层 (surface) 即为核心/归档/对话操作 (core/archival/conversation operations)。"
    },
    {
      "stage": "check",
      "correct": 1,
      "question": "What does the interrupt pattern do in MemGPT?",
      "questionZh": "MemGPT 中的中断模式（Interrupt Pattern）有什么作用？",
      "options": [
        "It halts the entire agent run on any error",
        "Mid-conversation the agent invokes a memory tool, the runtime executes it, and the result splices into the next turn",
        "It bypasses the context window entirely",
        "It triggers a model retrain"
      ],
      "optionsZh": [
        "遇到任何错误时，它将终止整个智能体 (Agent) 的运行。",
        "对话中途，智能体（Agent）调用记忆工具（Memory Tool），运行时（Runtime）执行该工具，并将结果拼接至下一轮对话（Turn）中。",
        "它完全绕过了上下文窗口 (context window)。",
        "这会触发模型重新训练 (Model Retraining)。"
      ],
      "explanation": "Memory-as-interrupt: invoke memory tool, runtime fetches, result returns as a new observation on the next turn (like a Unix read()).",
      "explanationZh": "内存中断机制 (Memory-as-interrupt)：调用记忆工具 (memory tool) 后，由运行时 (runtime) 执行数据获取，结果将在下一轮次 (turn) 作为新的观测 (observation) 返回（类似于 Unix `read()`）。"
    },
    {
      "stage": "check",
      "correct": 1,
      "question": "Which production system did MemGPT evolve into in 2024?",
      "questionZh": "MemGPT 在 2024 年演进为了哪个生产系统？",
      "options": [
        "Mem0",
        "Letta",
        "Zep",
        "Pinecone"
      ],
      "optionsZh": [
        "Mem0",
        "Letta",
        "Zep",
        "Pinecone"
      ],
      "explanation": "MemGPT became Letta in September 2024; the research repo cpacker/MemGPT still exists as the origin.",
      "explanationZh": "MemGPT 于 2024 年 9 月更名为 Letta；研究仓库 cpacker/MemGPT 仍作为原始仓库保留。"
    },
    {
      "stage": "post",
      "correct": 1,
      "question": "What is memory poisoning in this context?",
      "questionZh": "在此上下文中，什么是内存投毒（Memory Poisoning）？",
      "options": [
        "A bug that corrupts vector indices",
        "An attacker's content gets stored as a memory note and is re-ingested on future recalls",
        "Reading from disk when RAM is available",
        "Embedding model version drift"
      ],
      "optionsZh": [
        "导致向量索引（vector indices）损坏的缺陷（bug）",
        "攻击者的内容会被存储为记忆笔记（Memory Note），并在后续的检索（Recall）过程中被重新摄入（Re-ingested）。",
        "当内存 (RAM) 可用时从磁盘读取",
        "嵌入模型版本漂移 (Embedding Model Version Drift)"
      ],
      "explanation": "External memory is retrieved text; if attacker-reachable content lands in a memory note the agent re-ingests it next session.",
      "explanationZh": "外部记忆（External Memory）为检索文本；若攻击者可访问内容（Attacker-Reachable Content）落入记忆笔记（Memory Note）中，智能体（Agent）将在下一次会话中重新摄入（Re-ingest）该内容。"
    },
    {
      "stage": "post",
      "correct": 1,
      "question": "Why does the lesson say production memory systems are MemGPT variants?",
      "questionZh": "为什么课程中指出，生产环境记忆系统（Production Memory Systems）是 MemGPT 的变体？",
      "options": [
        "They use the same vector DB",
        "Letta, Mem0, Assistants threads, and Claude Agent SDK all run the two-tier (or more) paged-memory pattern; differences are operational shape",
        "MemGPT owns the trademark",
        "They share weights"
      ],
      "optionsZh": [
        "它们使用相同的向量数据库 (Vector Database)",
        "Letta、Mem0、Assistants 线程 (Assistants threads) 以及 Claude Agent SDK 均采用两级（或多级）分页内存模式 (two-tier (or more) paged-memory pattern)；它们之间的差异主要体现在运行形态 (operational shape) 上。",
        "MemGPT 拥有商标权。",
        "它们共享权重（weights）"
      ],
      "explanation": "Pick by operational shape, not pattern; all share the MemGPT page in/out skeleton.",
      "explanationZh": "应根据运行形态 (operational shape) 而非模式 (pattern) 进行选择；它们均共享相同的 MemGPT 页面输入/输出骨架 (page in/out skeleton)。"
    }
  ]
}